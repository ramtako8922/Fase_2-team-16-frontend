import React from 'react';
import { RaceBy } from '@uiball/loaders';
const LoaderLogin = () => {
	return (
		<>
			<div>
				<RaceBy
					size={300}
					lineWeight={5}
					speed={1}
					color='black'
				/>
			</div>
		</>
	);
};

export default LoaderLogin;
