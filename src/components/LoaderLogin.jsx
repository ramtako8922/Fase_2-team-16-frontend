import React from 'react';
import { RaceBy } from '@uiball/loaders';
const LoaderLogin = () => {
	return (
		<>
			<div>
				<RaceBy
					size={300}
					lineWeight={5}
					speed={1}
					color='white'
				/>
			</div>
		</>
	);
};

export default LoaderLogin;
